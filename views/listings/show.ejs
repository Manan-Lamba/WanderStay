<%- layout("/layouts/boilerplate") %>

    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card" style="width: 100%;">
                <img src="<%= listing.image.url %>" class="card-img-top" alt="<%= listing.title %>"
                    style="height: 500px;">
                <div class="card-body">
                    <h3 class="card-title">
                        <%= listing.title %>
                    </h3>
                    <ul class="list-group list-group-flush">
                        <li class="list-group-item">
                            <b><%= listing.description %></b>
                        </li>
                        <li class="list-group-item"><b>Price </b>
                            <%= Number(listing.price).toLocaleString('en-IN', {style: 'currency',currency: 'INR',maximumFractionDigits: 0}); %>
                            /night
                        </li>
                        <li class="list-group-item"><b>Location </b> <i class="fa-solid fa-location-dot"></i>
                            <%= listing.location %>, <%= listing.country %>
                        </li>
                    </ul>
                    <br>
                    <form method="GET" action="/listings/<%=listing._id%>/edit" class="d-inline me-2">
                        <button class="btn btn-outline-success">Edit Details</button>
                    </form>
                    <form method="POST" action="/listings/<%=listing._id%>?_method=DELETE" class="d-inline">
                        <button class="btn btn-outline-danger">Delete</button>
                    </form>
                </div>
            </div>
        </div>
    </div>